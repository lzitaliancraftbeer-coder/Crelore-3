import React, { useState, useEffect } from 'react';
import { Beer, CheckCircle, ChevronRight, Menu, X, Mail, Instagram, Facebook } from 'lucide-react';

const App = () => {
  const [isMenuOpen, setIsMenuOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 50);
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  const beers = [
    {
      id: 1,
      name: "Apocalypse",
      type: "Intensa & Decisa",
      description: "Una birra intensa e decisa, perfetta per chi cerca carattere e personalità.",
      color: "from-green-600/20 to-green-900/40",
      accent: "text-green-400",
      icon: "🍺"
    },
    {
      id: 2,
      name: "Gold Rush",
      type: "Golden Ale",
      description: "Fresca e dorata, ideale per ogni momento. Una celebrazione della purezza del malto.",
      color: "from-yellow-600/20 to-yellow-900/40",
      accent: "text-yellow-500",
      icon: "✨"
    },
    {
      id: 3,
      name: "Super Positive",
      type: "Double IPA",
      description: "Leggera ma sorprendente, con note aromatiche uniche e un'energia travolgente.",
      color: "from-red-600/20 to-red-900/40",      
      accent: "text-red-500",
      icon: "➕"
    }
  ];

  return (
    <div className="min-h-screen bg-[#0a0a0a] text-gray-100 font-sans selection:bg-yellow-500 selection:text-black">
      {/* Navigation */}
      <nav className={`fixed w-full z-50 transition-all duration-300 ${scrolled ? 'bg-black/80 backdrop-blur-md py-4 border-b border-white/10' : 'bg-transparent py-6'}`}>
        <div className="max-w-7xl mx-auto px-6 flex justify-between items-center">
          <div className="flex items-center gap-2 group cursor-pointer">
            <div className="w-10 h-10 rounded-full border-2 border-yellow-500 flex items-center justify-center font-bold text-xl text-yellow-500 group-hover:bg-yellow-500 group-hover:text-black transition-all">LC</div>
            <span className="text-xl font-bold tracking-tighter uppercase group-hover:text-yellow-500 transition-colors">Italian Craft Beer</span>
          </div>
          
          <div className="hidden md:flex gap-8 text-sm font-medium uppercase tracking-widest">
            <a href="#birre" className="hover:text-yellow-500 transition-colors">Le Birre</a>
            <a href="#storia" className="hover:text-yellow-500 transition-colors">La Storia</a>
            <a href="#perche" className="hover:text-yellow-500 transition-colors">Perché Noi</a>
            <a href="#contatti" className="px-5 py-2 border border-yellow-500 rounded-full hover:bg-yellow-500 hover:text-black transition-all">Contatti</a>
          </div>

          <button className="md:hidden" onClick={() => setIsMenuOpen(!isMenuOpen)}>
            {isMenuOpen ? <X size={28} /> : <Menu size={28} />}
          </button>
        </div>

        {/* Mobile Menu */}
        {isMenuOpen && (
          <div className="absolute top-full left-0 w-full bg-black/95 backdrop-blur-xl border-b border-white/10 p-8 flex flex-col gap-6 md:hidden animate-in fade-in slide-in-from-top-4">
            <a href="#birre" onClick={() => setIsMenuOpen(false)} className="text-xl font-bold">Le Birre</a>
            <a href="#storia" onClick={() => setIsMenuOpen(false)} className="text-xl font-bold">La Storia</a>
            <a href="#perche" onClick={() => setIsMenuOpen(false)} className="text-xl font-bold">Perché Noi</a>
            <a href="#contatti" onClick={() => setIsMenuOpen(false)} className="text-xl font-bold text-yellow-500">Contatti</a>
          </div>
        )}
      </nav>

      {/* Hero Section */}
      <section className="relative min-h-screen flex items-center pt-20 overflow-hidden">
        {/* Background Gradients */}
        <div className="absolute top-[-10%] right-[-10%] w-[50%] h-[50%] bg-yellow-500/10 blur-[120px] rounded-full"></div>
        <div className="absolute bottom-[-10%] left-[-10%] w-[50%] h-[50%] bg-red-600/10 blur-[120px] rounded-full"></div>

        <div className="max-w-7xl mx-auto px-6 w-full grid md:grid-cols-2 gap-12 items-center relative z-10">
          <div className="animate-in fade-in slide-in-from-left-8 duration-700">
            <span className="inline-block px-4 py-1.5 rounded-full bg-yellow-500/10 border border-yellow-500/20 text-yellow-500 text-xs font-bold uppercase tracking-[0.2em] mb-6">
              Eccellenza Artigianale Italiana
            </span>
            <h1 className="text-6xl md:text-8xl font-black mb-8 leading-[0.9] tracking-tight">
              BIRRA CHE <br />
              <span className="text-transparent bg-clip-text bg-gradient-to-r from-yellow-500 to-orange-400">LASCIA IL SEGNO.</span>
            </h1>
            <p className="text-xl text-gray-400 mb-10 max-w-lg leading-relaxed">
              Tradizione, innovazione e un carattere audace in ogni sorso. Scopri l'essenza di LC Italian Craft Beer attraverso le nostre creazioni uniche.
            </p>
            <div className="flex flex-wrap gap-4">
              <a href="#birre" className="px-8 py-4 bg-yellow-500 text-black font-bold rounded-xl hover:bg-yellow-400 hover:scale-105 transition-all flex items-center gap-2">
                Scopri le birre <ChevronRight size={20} />
              </a>
              <a href="#contatti" className="px-8 py-4 bg-white/5 border border-white/10 text-white font-bold rounded-xl hover:bg-white/10 transition-all">
                Contattaci
              </a>
            </div>
          </div>
          
          <div className="relative animate-in fade-in zoom-in duration-1000">
            <div className="absolute inset-0 bg-gradient-to-t from-[#0a0a0a] via-transparent to-transparent z-10"></div>
            <div className="relative p-8 rounded-3xl bg-white/5 border border-white/10 backdrop-blur-sm overflow-hidden">
                {/* SVG Mockup of multiple bottles as placeholder for images */}
                <div className="flex justify-center items-end h-[400px] gap-4">
                    <div className="w-16 h-64 bg-yellow-600/40 rounded-t-full border border-yellow-500/30"></div>
                    <div className="w-20 h-80 bg-red-600/40 rounded-t-full border border-red-500/30 animate-pulse"></div>
                    <div className="w-16 h-64 bg-green-600/40 rounded-t-full border border-green-500/30"></div>
                </div>
                <div className="absolute inset-0 flex items-center justify-center pointer-events-none opacity-20">
                    <Beer size={300} strokeWidth={0.5} />
                </div>
            </div>
          </div>
        </div>
      </section>

      {/* Sezione Chi Siamo */}
      <section id="storia" className="py-24 relative overflow-hidden">
        <div className="max-w-7xl mx-auto px-6">
          <div className="grid md:grid-cols-2 gap-16 items-center">
            <div className="order-2 md:order-1">
                <div className="relative">
                    <div className="aspect-square bg-gradient-to-br from-gray-800 to-black rounded-3xl border border-white/10 overflow-hidden">
                        <div className="absolute inset-0 flex items-center justify-center opacity-30 text-yellow-500">
                           <Beer size={160} />
                        </div>
                        <div className="absolute inset-0 bg-black/40 p-12 flex flex-col justify-end">
                            <h3 className="text-3xl font-bold text-white mb-2 italic">"Fatta per chi non si accontenta"</h3>
                        </div>
                    </div>
                </div>
            </div>
            <div className="order-1 md:order-2">
              <h2 className="text-4xl font-bold mb-8">La Nostra Identità</h2>
              <p className="text-xl text-gray-400 leading-relaxed mb-6 italic">
                “LC Italian Craft Beer nasce dalla passione per la birra artigianale e dalla voglia di creare qualcosa di unico.”
              </p>
              <p className="text-lg text-gray-400 leading-relaxed mb-10">
                Ogni birra è studiata per offrire un’esperienza autentica, con ingredienti selezionati e un’identità forte. Non facciamo solo birra, creiamo ricordi liquidi che celebrano l'eccellenza del territorio italiano con uno sguardo rivolto all'innovazione globale.
              </p>
              <div className="grid grid-cols-2 gap-8 border-t border-white/10 pt-10">
                <div>
                    <div className="text-4xl font-black text-yellow-500 mb-2">100%</div>
                    <div className="text-xs uppercase tracking-widest text-gray-500 font-bold">Artigianale</div>
                </div>
                <div>
                    <div className="text-4xl font-black text-yellow-500 mb-2">Qualità</div>
                    <div className="text-xs uppercase tracking-widest text-gray-500 font-bold">Premium</div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Sezione Birre */}
      <section id="birre" className="py-24 bg-zinc-900/30">
        <div className="max-w-7xl mx-auto px-6">
          <div className="text-center mb-20">
            <h2 className="text-4xl md:text-5xl font-black mb-4 uppercase">Le Nostre Specialità</h2>
            <div className="w-24 h-1 bg-yellow-500 mx-auto rounded-full"></div>
            <p className="mt-6 text-gray-400 max-w-2xl mx-auto">
              Ogni bottiglia racconta una storia di luppolo, malto e creatività pura. Scopri il carattere delle nostre etichette.
            </p>
          </div>

          <div className="grid md:grid-cols-3 gap-10">
            {beers.map((beer) => (
              <div key={beer.id} className="group relative">
                <div className={`absolute -inset-0.5 bg-gradient-to-b ${beer.color} rounded-[2rem] blur opacity-20 group-hover:opacity-100 transition duration-500`}></div>
                <div className="relative bg-[#0d0d0d] border border-white/10 p-8 rounded-[2rem] h-full flex flex-col hover:translate-y-[-8px] transition-transform duration-500 overflow-hidden">
                  <div className="mb-8 flex justify-between items-start">
                    <span className="text-4xl">{beer.icon}</span>
                    <span className={`text-[10px] font-bold uppercase tracking-[0.3em] py-1 px-3 bg-white/5 rounded-full ${beer.accent}`}>Limited Edition</span>
                  </div>
                  <h3 className="text-2xl font-black mb-1 uppercase group-hover:text-yellow-500 transition-colors">{beer.name}</h3>
                  <div className={`text-sm font-medium mb-6 ${beer.accent}`}>{beer.type}</div>
                  <p className="text-gray-400 leading-relaxed flex-grow">
                    {beer.description}
                  </p>
                  <button className="mt-8 flex items-center gap-2 font-bold text-sm uppercase tracking-widest group-hover:gap-4 transition-all">
                    Dettagli Tecnici <ChevronRight size={16} />
                  </button>
                  
                  {/* Decorative element background */}
                  <div className={`absolute -right-4 -bottom-4 w-24 h-24 rounded-full blur-[40px] opacity-10 ${beer.color}`}></div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Sezione Perché Sceglierci */}
      <section id="perche" className="py-24 relative">
        <div className="max-w-7xl mx-auto px-6">
          <div className="bg-gradient-to-br from-zinc-800/40 to-black/40 border border-white/10 rounded-[3rem] p-12 md:p-20 relative overflow-hidden">
            <div className="grid md:grid-cols-2 gap-16 items-center">
              <div>
                <h2 className="text-4xl font-bold mb-10 leading-tight italic">
                  Perché scegliere <br />
                  <span className="text-yellow-500 not-italic">LC Italian Craft Beer?</span>
                </h2>
                <div className="space-y-8">
                  {[
                    { title: "Ingredienti selezionati", desc: "Scegliamo solo i malti e i luppoli migliori per garantire una qualità senza compromessi." },
                    { title: "Produzione artigianale", desc: "Ogni cotta è seguita con attenzione maniacale, rispettando i tempi naturali di maturazione." },
                    { title: "Ricette originali", desc: "Non seguiamo le mode, creiamo blend unici studiati per sorprendere il palato." },
                    { title: "Brand forte e riconoscibile", desc: "Le nostre etichette riflettono la nostra anima: moderna, grafica e audace." }
                  ].map((item, i) => (
                    <div key={i} className="flex gap-4">
                      <div className="flex-shrink-0 w-6 h-6 rounded-full bg-yellow-500/20 flex items-center justify-center">
                        <CheckCircle size={16} className="text-yellow-500" />
                      </div>
                      <div>
                        <h4 className="font-bold mb-1">{item.title}</h4>
                        <p className="text-gray-400 text-sm leading-relaxed">{item.desc}</p>
                      </div>
                    </div>
                  ))}
                </div>
              </div>
              <div className="relative group">
                <div className="absolute inset-0 bg-yellow-500/10 rounded-2xl rotate-3 group-hover:rotate-0 transition-transform"></div>
                <div className="relative aspect-square md:aspect-[4/5] bg-zinc-800 rounded-2xl border border-white/20 overflow-hidden transform group-hover:scale-105 transition-transform">
                    {/* Placeholder for production/shelf photo */}
                    <div className="absolute inset-0 bg-black/40 z-10 flex items-center justify-center p-10 text-center">
                        <span className="text-white/20 uppercase tracking-[0.5em] font-black text-2xl">Craft Culture</span>
                    </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* CTA Finale */}
      <section className="py-24 text-center relative overflow-hidden">
        <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] bg-yellow-500/10 blur-[120px] rounded-full opacity-50"></div>
        <div className="max-w-4xl mx-auto px-6 relative z-10">
          <Beer size={64} className="mx-auto text-yellow-500 mb-8 animate-bounce" />
          <h2 className="text-4xl md:text-6xl font-black mb-8 leading-tight">
            PRONTO A LASCIARTI <br />CONQUISTARE?
          </h2>
          <p className="text-xl text-gray-400 mb-12">
            Scopri il mondo LC Italian Craft Beer e lasciati conquistare da sapori unici e inimitabili.
          </p>
          <div className="flex flex-col sm:flex-row gap-6 justify-center">
            <button className="px-12 py-5 bg-white text-black font-black rounded-2xl hover:bg-yellow-500 hover:scale-105 transition-all text-lg uppercase tracking-widest shadow-xl shadow-white/5">
              Contattaci Ora
            </button>
            <button className="px-12 py-5 bg-transparent border-2 border-white/10 text-white font-bold rounded-2xl hover:bg-white/5 transition-all text-lg uppercase tracking-widest">
              Store Locator
            </button>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer id="contatti" className="py-12 border-t border-white/5 bg-black/50">
        <div className="max-w-7xl mx-auto px-6">
          <div className="grid md:grid-cols-4 gap-12 mb-16">
            <div className="col-span-2">
              <div className="flex items-center gap-2 mb-6">
                <div className="w-8 h-8 rounded-full border border-yellow-500 flex items-center justify-center font-bold text-sm text-yellow-500">LC</div>
                <span className="text-lg font-bold tracking-tighter uppercase">Italian Craft Beer</span>
              </div>
              <p className="text-gray-500 max-w-sm mb-8">
                L'eccellenza della birra artigianale italiana, prodotta con passione e dedizione per offrire un'esperienza sensoriale superiore.
              </p>
              <div className="flex gap-4">
                <a href="#" className="w-10 h-10 rounded-full bg-white/5 border border-white/10 flex items-center justify-center hover:bg-yellow-500 hover:text-black transition-all">
                  <Instagram size={18} />
                </a>
                <a href="#" className="w-10 h-10 rounded-full bg-white/5 border border-white/10 flex items-center justify-center hover:bg-yellow-500 hover:text-black transition-all">
                  <Facebook size={18} />
                </a>
                <a href="#" className="w-10 h-10 rounded-full bg-white/5 border border-white/10 flex items-center justify-center hover:bg-yellow-500 hover:text-black transition-all">
                  <Mail size={18} />
                </a>
              </div>
            </div>
            
            <div>
              <h4 className="font-bold mb-6 uppercase tracking-widest text-xs text-yellow-500">Esplora</h4>
              <ul className="space-y-4 text-gray-500 text-sm font-medium">
                <li><a href="#birre" className="hover:text-white transition-colors">Le Nostre Birre</a></li>
                <li><a href="#storia" className="hover:text-white transition-colors">La Nostra Storia</a></li>
                <li><a href="#" className="hover:text-white transition-colors">Eventi</a></li>
                <li><a href="#" className="hover:text-white transition-colors">Blog</a></li>
              </ul>
            </div>

            <div>
              <h4 className="font-bold mb-6 uppercase tracking-widest text-xs text-yellow-500">Contatti</h4>
              <ul className="space-y-4 text-gray-500 text-sm font-medium">
                <li>Italia, Sede Artigianale</li>
                <li>info@lc-craftbeer.it</li>
                <li>+39 012 345 6789</li>
                <li className="text-yellow-500 underline underline-offset-4">Lavora con noi</li>
              </ul>
            </div>
          </div>
          
          <div className="pt-8 border-t border-white/5 flex flex-col md:row justify-between items-center gap-6 text-xs font-bold text-gray-600 uppercase tracking-widest">
            <p>© 2024 LC Italian Craft Beer. All Rights Reserved.</p>
            <div className="flex gap-8">
              <a href="#" className="hover:text-gray-300 transition-colors">Privacy Policy</a>
              <a href="#" className="hover:text-gray-300 transition-colors">Cookie Policy</a>
            </div>
          </div>
        </div>
      </footer>
    </div>
  );
};

export default App;
